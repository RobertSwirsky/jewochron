<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="Jewochron.Views.SettingsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Jewochron.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="#1E1E1E">
    
    <ScrollViewer>
        <StackPanel Spacing="24" Padding="24">
            <StackPanel Orientation="Horizontal" Spacing="12">
                <Button Content="← Back" Click="BackButton_Click" FontSize="16" />
                <TextBlock Text="Settings" FontSize="32" FontWeight="Bold" VerticalAlignment="Center" />
            </StackPanel>

            <Border Background="#2D2D30" CornerRadius="12" Padding="16" Margin="0,0,0,12">
                <StackPanel>
                    <TextBlock Text="Yahrzeits" FontSize="24" FontWeight="SemiBold" Margin="0,0,0,8" />
                    <TextBlock Text="Manage memorial dates for loved ones" FontSize="14" Foreground="#888888" Margin="0,0,0,12" />
                    <Button Content="Manage Yahrzeits" Click="ManageYahrzeits_Click" />
                </StackPanel>
            </Border>

            <Border Background="#2D2D30" CornerRadius="12" Padding="16" Margin="0,0,0,12">
                <StackPanel>
                    <TextBlock Text="Simchas" FontSize="24" FontWeight="SemiBold" Margin="0,0,0,8" />
                    <TextBlock Text="Manage joyous occasions and celebrations" FontSize="14" Foreground="#888888" Margin="0,0,0,12" />
                    <Button Content="Manage Simchas" Click="ManageSimchas_Click" />
                </StackPanel>
            </Border>
        </StackPanel>
    </ScrollViewer>
</Page>
